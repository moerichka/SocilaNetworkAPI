const router = require("express").Router();
const User = require("../models/user");

router.get("/", (req, res) => {

    // -- ОБНОВИТЬ ПОЛЬЗОВАТЕЛЯ --
    // -- УДАЛИТЬ ПОЛЬЗОВАТЕЛЯ --
    // -- ПОЛУЧИТЬ ПОЛЬЗОВАТЕЛЯ --
    // -- ПОДПИСАТЬСЯ НА ПОЛЬЗОВАТЕЛЯ --
    // -- ОТПИСАТЬСЯ ПОЛЬЗОВАТЕЛЯ --
});

module.exports = router;
